# Server configuration
PORT=3001

# Allow CORS from these origins (comma-separated list)
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-domain.vercel.app

# Uncomment if you need to set a specific host
# HOST=0.0.0.0
